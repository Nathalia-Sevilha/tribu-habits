  <h1>Your communities</h1>
  <% @communities.each do |community| %>
  <div class="community_card text-center m-3">
  <p><%= link_to community.title, community_path(community) %></p>
  </div>
  <% end %>
