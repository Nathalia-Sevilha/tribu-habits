<%= render partial: "shared/header",  locals: { zone: @habits, title: "Habits"}  %>
<%= link_to "Add custom habit", new_habit_path, class: "btn btn-large m-3 p-3" %>

<div>
  <% @lists.each do |item| %>
    <%= link_to new_habit_path(habit_id: item.id), class: "community_card d-flex justify-content-between align-items-center m-3" do %>
      <%= item.title %>
      <p>+</p>
    <% end %>
  <% end %>
</div>
